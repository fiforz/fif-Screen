# 更新日志

本项目按语义化版本记录用户可见的变化。

## 未发布

## 0.5.5 - 2026-07-14

### 修复

- Windows Host 不再依赖 `DrawIconEx` 将系统光标合成到扩展屏视频帧，改为直接写入高对比度软件箭头，解决部分 Windows 10 电脑上光标坐标和绘制接口均报告成功、但 Android 扩展屏仍看不到鼠标的问题；
- Host 使用 Per-Monitor V2 DPI 感知和物理光标坐标，并在 FifScreen 位于主屏左侧或上方、Windows 返回访问拒绝及屏内局部坐标时进行受限换算，避免负坐标拓扑下误判光标位于扩展屏外；
- 增加光标查询、坐标判断和绘制状态日志，以及负坐标换算和软件光标像素回归测试。

### 验证

- 在两台 Windows 10 电脑上分别验证主屏光标不闪烁，以及位于主屏左侧的 FifScreen 扩展屏能够稳定显示软件鼠标；
- Windows Release、协议、控制中心、加密、触控、Socket、光标坐标和软件光标测试通过；
- Android 单元测试、Lint、Debug APK、显示驱动 Signability 与 Development 安装包验证通过。

## 0.5.4 - 2026-07-13

### 修复

- Windows Host 现在只从 FifScreen 虚拟显示器的专用 DC 捕获画面，并移除会强制桌面合成层参与每帧抓取的 `CAPTUREBLT`，解决 USB 扩展屏运行时本机主屏鼠标指针闪烁；同时缓存已验证的光标图像并容忍短暂查询失败，避免 Android 画面中的鼠标指针闪烁。
- Windows Host 的控制、视频和发现端口改为独占绑定，避免多个 Host 同时监听时 USB 控制与视频连接被分配给不同进程、造成 Android 已连接但没有画面。

## 0.5.3 - 2026-07-13

### 修复

- 修复部分 Windows 电脑将 FifScreen 虚拟显示器置于复制拓扑，导致驱动和 Monitor 均已创建但 Android 始终收不到可捕获扩展桌面的问题；
- 控制中心现在等待驱动与虚拟显示器就绪，并通过 DisplayConfig 主动切换、校验独立扩展桌面后才启动 Host 和 Android；
- Host 通过 FifScreen 软件设备的 DisplayConfig adapter path 定位捕获源，不再依赖本地化显示名称，也不会在复制拓扑下误选物理主屏；
- 修复 GUI 操作回调遮蔽顶层连接模式变量，导致选择无线局域网并应用 PIN 后仍以 USB 模式启动 Host 的问题；
- 修复源码控制中心在多个同版本构建产物并存时固定选中旧 Launcher/Host，进而把正常 USB 驱动误报为异常的问题；
- Launcher 状态协议不兼容时现在明确提示重新构建或安装，不再伪装成驱动故障。

### 诊断与验证

- 控制中心记录请求模式、实际 Host 路径、PID 和启动参数，但不记录 PIN；Host 输出最终 transport 与 ADB reverse 状态；
- 新增控制中心 GUI 模式与同版本最新构建选择回归测试；
- 在真实 Windows 设备上验证独立扩展拓扑、USB 双 ADB reverse、Android 控制/视频握手与触控；
- 通过 Windows Release 构建、协议/PIN/触控测试、Android 单元测试、Lint、APK、驱动 Signability 和 Development 安装包验证。

## 0.5.2 - 2026-07-12

### 修复

- 修复部分电脑上软件显示设备已经创建、但 Windows 尚未完成显示枚举时，主机立即关闭 USB 视频通道并导致 Android 只提示 `socket closed` 的问题；
- 修复控制中心版本和 Android 版本一致、但实际选中了旧版 Host 时仍继续启动 Android，最终被误报为“电脑关闭了控制通道”的问题；
- Windows 选择 USB 或无线模式时现在会把连接方式明确传给 Android，避免 Android 沿用上次无线偏好而阻断 USB；
- Windows 主机现在等待扩展显示真正可捕获后再开始视频流，并为显示等待开始、成功和超时输出明确诊断事件；
- Windows 控制中心分别配置并校验 USB 控制与视频端口的 ADB reverse，映射失败时不再错误地记录“已配置”；
- Android 按发现、PIN、控制握手、视频认证、视频启动和传输阶段显示中文错误，正常断线继续自动重连，不再向用户暴露底层 socket 文本；
- 修复拔掉 USB 后局域网发现可能被移动数据、VPN 或厂商网络栈路由到错误接口的问题。

### 改进

- Android 局域网发现和后续 TCP 连接会绑定到 Wi-Fi 或以太网，并保留默认网络回退；
- Android 增加手动电脑 IPv4 连接方式，可绕过不转发广播的路由器或 Android 设备；
- Windows 控制中心增加“应用 PIN 并等待连接”按钮，并显示可在 Android 手动输入的本机 IPv4 地址；
- 源码运行时严格选择与当前版本一致的 Host 和 Launcher，找不到匹配产物会在启动 Android 前明确失败；
- Android 16 无线连接按系统要求申请附近设备权限，USB 模式不受影响。

### 验证

- 在真实 Android 设备上验证双 ADB reverse、USB 控制握手、1920 x 1080 约 49–50 FPS 视频、无扩展屏时 15 秒等待、中文错误提示和持续自动重连；
- 通过 Android 单元测试、APK 构建、Lint、MSVC Windows 主机构建及协议和 PIN 加密测试。

## 0.5.0 - 2026-07-11

### 新增

- Windows 控制中心和 Android 应用支持手动选择 USB 调试或无线局域网连接；
- 同一局域网内通过 UDP 自动发现电脑，Windows 与 Android 输入相同四位 PIN 即可握手连接；
- 无线控制通道使用 PBKDF2-HMAC-SHA256 挑战响应和双向证明，视频通道使用一次性会话证明；
- 预留 Android 服务器端点目录、Windows 中继传输枚举和服务器 URL 配置接口，便于后续增加互联网中继。

### 改进

- 无线模式复用现有 1920 x 1080、目标 50 FPS H.264 和原生触控链路；
- Windows 防火墙规则只允许本地子网访问 UDP 发现和 TCP 传输端口，完整卸载时自动清理；
- Android 连接设置在启动、断线或按返回键时可访问，PIN 仅保存在进程内存中；
- USB 模式继续只监听 `127.0.0.1` 并使用原有 ADB reverse，不增加无线配对开销。

### 验证

- 在真实 Android 设备与 Windows 电脑直连局域网中验证 PIN 接受/拒绝、1920 x 1080 约 50 FPS 视频和触控；
- 验证从无线模式切回 USB 后，原有画面、触控、Owner 和 Software Device 均保持正常。

## 0.4.1 - 2026-07-11

### 修复

- 修复源码控制中心可能启动无触控能力的旧版 Windows 主机，导致手机触摸无响应的问题；
- 控制中心现在校验主机和设备启动器的文件版本，并优先使用与当前版本一致的构建产物；
- 检测到旧主机时只重启主机进程，保留现有扩展显示 Owner 和 Software Device，避免重复创建设备；
- Android 触摸视图现在始终接管完整手势，不再因发送线程瞬时未就绪而丢失后续移动或抬起事件。

### 改进

- 增加触摸握手能力、输入就绪状态和手势发送结果的结构化诊断日志；
- 在真实 Android 设备、1920 x 1080 FifScreen 扩展屏上验证 `WM_POINTER` 和鼠标兼容消息。

## 0.4.0 - 2026-07-11

### 新增

- Android 扩展屏支持原生单指和多指触控；
- 支持点击、拖动、长按以及由 Windows 应用处理的双指缩放和滚动手势；
- Windows 主机使用系统 `InjectTouchInput` 将触点注入 FifScreen 扩展显示器；
- 新增触控协议、坐标映射、单指、多指和断线取消测试。

### 改进

- Android 使用独立低延迟输入发送线程，并合并连续移动帧，避免触控积压；
- 握手增加触控能力协商，新旧 Android 和 Windows 组件可以兼容混用；
- 控制连接断开时自动取消所有活动触点，避免残留按下状态。

## 0.3.2 - 2026-07-10

### 修复

- 修复 GitHub Release 静态清单以二进制响应返回时，Windows PowerShell 5.1 无法解析 JSON、错误提示连接失败的问题；
- 静态清单现在先按 UTF-8 显式解码，再进行字段和 SHA-256 校验；
- 更新检查显式启用 TLS 1.2，并关闭 PowerShell 下载进度渲染以减少网络请求阻塞。

## 0.3.1 - 2026-07-10

### 新增

- Windows 控制中心启动时检查管理员权限；
- 非管理员启动时提示“请以管理员身份运行”，确认后立即退出；
- GitHub Release API 不可用时回退到最新 Release 的静态更新清单资产。

### 修复

- 修复网络正常但 GitHub API 间歇性超时导致手动检查立即报错的问题；
- 手动检查更新增加三次重试，只有 API 和静态清单都失败才显示网络提示；
- 后台检查继续使用静默退避重试，不打扰用户。

## 0.3.0 - 2026-07-10

### 新增

- Windows 控制中心启动时自动检查 GitHub Releases 最新版本；
- 更新提示显示版本号、发布时间和完整更新日志；
- 更新安装包自动下载、SHA-256 校验、运行时关闭和提权启动；
- GitHub 连接失败时后台静默退避重试；
- “关于”菜单、“检查更新”、“使用教程”和产品信息；
- 中文 Android 开发者模式、USB 调试和扩展屏使用教程；
- 可重复执行的 GitHub Release 发布脚本。

### 改进

- Windows 控制端按钮、状态和主要日志全面中文化；
- 安装器快捷方式、开发驱动警告和维护提示中文化；
- 安装包默认写入官方 GitHub Releases API 更新地址；
- README 改为完整中文项目说明。

## 0.2.1 - 2026-07-10

### 新增

- Windows Media Foundation H.264 编码链路；
- Android MediaCodec 硬件 H.264 解码和低延迟参数；
- 1920 × 1080、目标 50 FPS、16 Mbps 视频模式；
- 最新帧优先的有界捕获管线和延迟统计；
- 集成 Windows 主机、驱动、ADB 和 Android APK 的版本化安装包。

### 修复

- 改进扩展屏系统鼠标显示；
- 修复 USB 重连与 Windows 控制端启动异常；
- 降低高动态画面播放时的卡顿和延迟积累。

## 0.2.0 - 2026-07-10

### 新增

- 首个带统一版本号的 Windows x64 一键安装包；
- 安装包集成 Windows 主机、间接显示驱动、软件设备启动器、ADB 和 Android APK；
- 预留更新配置、运行时停止脚本和完整卸载接口；
- Windows 控制中心支持启动、停止、重连和状态刷新。

### 安全

- 开发版驱动使用测试签名，并在安装时明确提示测试签名环境要求；
- 安装程序不会自动修改 BCD、安全启动或重启配置。
